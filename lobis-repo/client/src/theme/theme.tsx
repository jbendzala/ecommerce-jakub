import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#5c5ec9',
      light: '#6d6fe9',
      dark: '#4e50ad',
    },
    secondary: {
      main: '#cdcdfd',
      light: '#d4d5f2',
      dark: '#afafed',
    },
  },
});

export default theme;
